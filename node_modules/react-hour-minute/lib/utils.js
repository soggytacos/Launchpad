'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
var generateHours = exports.generateHours = function generateHours() {
  return Array.from(Array(24).keys()).map(function (v) {
    v = '' + v;
    if (v.length === 1) v = '0' + v;
    return v;
  });
};

var generateMinutes = exports.generateMinutes = function generateMinutes() {
  return Array.from(Array(60).keys()).map(function (v) {
    v = '' + v;
    if (v.length === 1) v = '0' + v;
    return v;
  });
};